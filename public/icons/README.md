# Public Icons Folder

Place your SVG/PNG icons and favicons here.

For example, to reference a custom SVG icon:
```jsx
<img src="/icons/custom-dashboard.svg" alt="Dashboard Icon" />
```

To update the website favicon, place a `favicon.ico` or a `icon.png` directly in the `app/` folder or root of this `public/` directory.
